import { FilePlusIcon } from "@/components/icons";
import type { IconProps } from "@chakra-ui/react";
import type React from "react";

export const DataBubbleIcon = (props: IconProps): JSX.Element => (
  <FilePlusIcon color="blue.500" {...props} />
);
